import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  devIndicators: false,     // To remove the "Development" banner in the bottom left corner during development
};

export default nextConfig;
